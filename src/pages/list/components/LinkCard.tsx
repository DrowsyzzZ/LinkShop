import { Linkshop } from "@/types/linkshopType";

interface LinkCardProps {
  info: Linkshop;
}

const LinkCard = ({ info }: LinkCardProps) => {
  return <div>링크 카드</div>;
};

export default LinkCard;
